<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project2">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
        <activity android:name=".ViewNotepad" /> <!-- Info와 SettingPassword는 팝업형식으로 띄움 -->
        <activity
            android:name=".Info"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".SettingPassword"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" /> <!-- 초기화면은 EnterPassword로 잡았음. -->
        <activity android:name=".EnterPassword" />
        <activity android:name=".Edit_Notepad" />
        <activity android:name=".NewNotepad" />
        <activity android:name=".NoteAdapter" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>